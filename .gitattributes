# 모든 쉘 스크립트는 LF 로 강제
*.sh text eol=lf

# Dockerfile은 LF 유지
Dockerfile text eol=lf

# 일반 텍스트 파일은 자동 변환
*.txt text

# 윈도우/맥 환경에서도 바이너리 파일은 변환 안 함
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.jar binary
