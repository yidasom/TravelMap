## 데이터 저장용 볼륨
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gke-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard-rwo   # Autopilot 기본 StorageClass