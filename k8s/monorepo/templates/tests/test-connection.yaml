apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monorepo.fullname" . }}-test-connection"
  labels:
    {{- include "monorepo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monorepo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
