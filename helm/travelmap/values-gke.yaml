replicaCount: 2

backend:
  image:
    repository: somlh1212/travelmap-backend
    tag: v1.0.0
    pullPolicy: Always
  service:
    type: ClusterIP # 내부용
    port: 8080
  env:
    SPRING_PROFILES_ACTIVE: prod
    DB_HOST: postgres.default.svc.cluster.local
    DB_PORT: "5432"
    DB_USERNAME: travelmap
  secrets: # 시크릿 정보를 한 곳에 모아 관리
    dbPassword:
      secretName: travelmap-external-secret
      secretKey: db-password
    youtubeApiKey:
      secretName: travelmap-secret
      secretKey: youtube-api-key
  resources:
    requests:
      cpu: "500m"
      memory: "512Mi"
    limits:
      cpu: "1000m"
      memory: "1024Mi"

frontend:
  image:
    repository: somlh1212/travelmap-frontend
    tag: v1.0.0
    pullPolicy: Always
  service:
    type: LoadBalancer  # 외부용
    port: 80
  resources:
    requests:
      cpu: "400m"
      memory: "512Mi"
    limits:
      cpu: "800m"
      memory: "1024Mi"

db:
  enabled: false
  type: internal
  host: postgres.default.svc.cluster.local
  port: 5432
  username: travelmap
  password:
    secretName: travelmap-external-secret
    secretKey: db-password

imagePullSecrets:
  - name: dockerhub-credentials

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
# runAsNonRoot: true
# runAsUser: 1000

ingress:
  enabled: false
  className: ""
  annotations: {}
  # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

#resources:
#  # We usually recommend not to specify default resources and to leave this as a conscious
#  # choice for the user. This also increases chances charts run on environments with little
#  # resources, such as Minikube. If you do want to specify resources, uncomment the following
#  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
#   limits:
#     cpu: 100m
#     memory: 100Mi
#   requests:
#     cpu: 200m
#   memory: 200Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}