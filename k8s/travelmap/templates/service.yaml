apiVersion: v1
kind: Service
metadata:
  name: {{ include "travelmap.fullname" . }}
  labels:
    {{- include "travelmap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "travelmap.selectorLabels" . | nindent 4 }}
